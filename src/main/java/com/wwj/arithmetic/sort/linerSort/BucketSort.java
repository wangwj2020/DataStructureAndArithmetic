package com.wwj.arithmetic.sort.linerSort;

/**
 * 桶排序
 * 基本思想：将需要排序的数据分别装到几个有序的桶里，再对桶里数据排序。最后分别把桶里数据拿出来后放在一起就是有序的里
 * 时间复杂度：O（N），最坏 nLogn
 * 使用前提：数据能够很容易划分为多个桶，每个桶数据量不同差距过大
 * 适用场景：桶排序比较适合用在外部排序中
 */
public class BucketSort {
}
